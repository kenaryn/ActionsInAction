on: 
  pull_request_target
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  triage:
    runs-on: ubuntu-latest
    steps: 
      - name: Labeler
        uses: actions/labeler@v5.0.0
      
      - uses: devops-actions/actionlint@v0.1.3
          
          
